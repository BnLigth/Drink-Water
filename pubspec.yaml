name: su_ic
description: Su takibi uygulamasi.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Kodda kullandığımız kütüphaneler:
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  fl_chart: ^0.63.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
